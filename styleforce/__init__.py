"""styleforce -- shared GritQL rules for enforcing source-code style.

The ``.grit`` patterns ship inside this package so an install carries them with
no source checkout. Run them with the ``grit`` CLI; the repository's pytest
suite exercises each pattern's Markdown samples that way.
"""
